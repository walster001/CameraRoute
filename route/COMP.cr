puts get "av.component"
puts get "hdmi.component"
puts get "comp.component"
puts get "vga.component"

public class COMP {

 if comp.component (/="1") return error null
    else comp.component(connect.initiate);
}

private class lostConnection {
if comp.component = "0" wait(2); then comp.component.reconnect;
   }
}

private class purpleFix {
if comp.component (res="4:3") puts (res="16:9);
}
